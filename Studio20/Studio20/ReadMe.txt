Studio 20 ReadMe.txt
 1. Veda Bhalla, Ellie Ertl, Sophie Schochet
2. We need to delete the pointer to the actual file because it is password protected and once the function executes, the user should not retain access to the file.
 There is no Q3
4. We made a TextFile with a passwordProxy that had a password including numbers and letters. We wrote to the file “a b c” and then were able to read the same characters from the file, each time after entering the matching password to access the operations. This operates exactly as expected. If we entered an incorrect password, the program returned a value that corresponded to the message “incorrectPassword” in our error enumeration. This behavior is exactly as expected.
